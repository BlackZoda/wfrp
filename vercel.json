{
    "outputDirectory": "dist",
    "installCommand": "npm install",
    "buildCommand": "npm run build",
    "devCommand": "npm run start",
    "routes": [
      { "src": "/logout", "dest": "/api/edge" },
      { "src": "/protected/(.*)", "dest": "/api/edge" },
      { "handle": "filesystem" },
      { "src": "/(.*)", "dest": "/404" }
    ]
}